# =================================================
# IDE 및 편집기 관련 설정 파일
# 팀원 간에 다른 IDE 설정을 공유하지 않습니다.
# =================================================
.idea/
.vscode/
*.iml
*.ipr
*.iws
.project
.classpath
.factorypath
.settings/
.sts4-cache/

# =================================================
# 빌드 결과물 및 Gradle 캐시
# 이 파일들은 원본 코드가 아니며, 각자의 컴퓨터에서 생성해야 합니다.
# =================================================
build/
.gradle/
out/
bin/

# =================================================
# 운영체제(OS) 자동 생성 파일
# 프로젝트와 관련 없는 시스템 파일은 무시합니다.
# =================================================
.DS_Store
Thumbs.db
._*

# =================================================
# 로그 및 임시 파일
# 실행 로그나 임시 파일은 공유하지 않습니다.
# =================================================
*.log
*.tmp

# =================================================
# 민감한 정보 및 환경 설정 파일 (매우 중요!)
# API 키, 비밀번호, 로컬 DB 설정 등은 절대 공유하지 않습니다.
# =================================================
.env
application-local.yml
application-dev.yml
application-prod.yml

# 만약 .properties 파일을 사용한다면 아래 주석을 푸세요.
# application-local.properties
# application-dev.properties
# application-prod.properties

# =================================================
# 패키징된 실행 파일
# 빌드하면 자동으로 생성되므로, Git에 포함하지 않습니다.
# =================================================
*.jar
*.war
*.ear